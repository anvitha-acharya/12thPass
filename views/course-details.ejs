<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= course.name %> Details</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <header class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">12th Pass</a>
        </div>
    </header>

    <main class="container my-4">
        <h1><%= course.name %></h1>
        <p><%= course.short_description || 'No description available.' %></p>

        <div>
            <h4>Course Details</h4>
            <ul>
                <li><strong>Start Date:</strong> <%= course.start || 'N/A' %></li>
                <li><strong>End Date:</strong> <%= course.end || 'N/A' %></li>
                <li><strong>Language:</strong> <%= course.language || 'N/A' %></li>
                <li><strong>Enrollment URL:</strong> <a href="<%= course.enrollment_url %>" target="_blank">Enroll Now</a></li>
            </ul>
        </div>
    </main>
</body>
</html>
